# 게시글 마크다운 동기화

- 개요:
  - 웹에서 관리하는 문서는 별도의 DB가 있음. (DB가 가장 우선하며 가장 중요함)
  - 웹에서 문서를 작성하고 저장하면 DB에도 저장되지만, 마크다운 파일도 갱신되어야 함.
  - 대시보드에서 현재 마크다운 파일과 내용이 다르면 표시함.
    - 마크다운에서 대시보드로 갱신할 지, 아니면 대시보드에서 마크다운으로 갱신할 지 선택 버튼 제공
- 대시보드 긴 글 페이지 참조:
  - 대시보드는 개발 모드에서만 접근 가능함
  - 해당 페이지 컴포넌트: `src/routes/dashboard/p/[postId]/+page.svelte`
- 대시보드에서 글을 저장하면 현재 프로젝트 루트 경로에 `posts` 폴더에서 관리
  - `글제목__글ID/index.md` 파일로 저장함.(예: `Git_핵심_명령어_모음__PcUkdT/index.md`)
  - 글을 저장할 때:
    - 동기화 파일은 글의 ID로 검색해서 찾거나 없으면 생성.
    - 찾은 동기화 파일의 제목이 저장할 때의 글 제목과 다르면, 저장할 때의 제목으로 파일 이름을 갱신함.
    - 글이 배포 전이라면, `posts/longs` 폴더에 있는 해당 파일을 찾아 `posts/drafts` 폴더로 이동.
    - 글이 배포 상태라면, `posts/drafts` 폴더에 있는 해당 파일을 찾아 `posts/longs` 폴더로 이동.
- 대시보드에서 `마크다운에서 가져오기` 버튼을 선택하면 자동으로 해당 파일을 가져와서 웹에서 수정할 수 있도록 갱신함.
  - 가져오기를 완료하면 알림 메시지 표시(알림 컴포넌트 있음)
  - 저장은 대시보드에서 사용자가 직접 처리하니 자동 저장되지 않도록 주의!
- 내용:
